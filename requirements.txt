# Runtime deps for the hosted order-book logger on Railway.
# (Research/modelling deps live in pyproject.toml; the logger needs only these.)
websockets>=12
requests>=2.31
cryptography>=42
psycopg2-binary>=2.9
